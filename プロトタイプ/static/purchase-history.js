const savedOrders=JSON.parse(localStorage.getItem("reTailorPurchaseHistory")||"[]");
const sampleHistory=window.reTailorProducts.filter((item)=>[1,3].includes(item.id)).map((item,index)=>({...item,date:index?"2026/08/30 10:15":"2026/09/18 14:32",receipt:index?"RT-20260830-014":"RT-20260918-001",subtotal:item.price,total:item.price}));
const history=savedOrders.length?savedOrders.flatMap((order)=>order.items.map((item)=>({...item,date:order.date,receipt:order.receipt,subtotal:item.price,total:item.price}))):sampleHistory;
const yen=(value)=>`¥${value.toLocaleString()}`;
document.getElementById("historyList").innerHTML=history.map((item)=>`<article class="history-card"><div class="history-image" role="img" aria-label="${item.name}の商品画像">${item.category}</div><div class="history-content"><div class="order-meta"><span>注文日時 ${item.date}</span><span>レシート番号 ${item.receipt}</span></div><h2>${item.name}</h2><p class="seller">出品者：${item.seller}</p><dl><div><dt>カテゴリ</dt><dd>${item.category}</dd></div><div><dt>サイズ</dt><dd>${item.size}</dd></div><div><dt>カラー</dt><dd>${item.color}</dd></div></dl><p class="amount">小計 ${yen(item.subtotal)}　<strong>合計 ${yen(item.total)}</strong></p></div><a class="detail-link" href="purchase-history-detail.html?id=${item.id}">購入詳細を見る</a></article>`).join("");
